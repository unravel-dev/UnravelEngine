/*
 * The screen-probe trace: one 8x8 group per traced probe, one thread per octahedral
 * texel, all 64 rays fresh every frame. Body: gi_screen_probe_trace_kernel.sh.
 */

#include "gi/gi_screen_probe_trace_kernel.sh"
