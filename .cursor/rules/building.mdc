---
description: Build instructions for C and C++ source and CMake files
globs: "**/*.c,**/*.cc,**/*.cpp,**/*.cxx,**/*.h,**/*.hpp,**/CMakeLists.txt,**/*.cmake,**/conanfile.txt,**/Makefile"
alwaysApply: false
---

# Building

- Prefer CMake commands.
- First check whether an existing configured build directory is available.
- Reuse the existing generator and configuration where possible.
